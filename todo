# automating dev environment


# limitation
-does not include window management config like i3
-only for terminal setup in ubuntu

# apps used
-curl
-wget
-git

3. zsh
-install
-make zsh default shell
-install zap plugin manager
